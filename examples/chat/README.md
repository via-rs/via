# Chat

The chat example uses tokio-tungstenite as the default websocket backend. If
you would like to use tokio-websockets instead, execute the following:

```
cargo run --no-default-features --features tokio-websockets
```
